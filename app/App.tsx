import { SafeAreaView } from "react-native";
import { Typography } from "@/components";

const App = () => {
  return (
    <SafeAreaView>
      <Typography variant="heading1">Hello World</Typography>
      <Typography variant="heading2">Hello World</Typography>
      <Typography variant="heading3">Hello World</Typography>
      <Typography variant="heading4">Hello World</Typography>
      <Typography variant="heading5">Hello World</Typography>
      <Typography variant="body1">Hello World</Typography>
      <Typography variant="body2">Hello World</Typography>
    </SafeAreaView>
  );
};

export default App;
